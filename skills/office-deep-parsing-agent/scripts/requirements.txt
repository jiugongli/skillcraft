# Core parser dependency.
openpyxl>=3.1

# Optional but recommended for visual/OCR and richer Office parsing.
pillow>=10
# Optional wrapper; runtime falls back to tesseract CLI when absent.
pytesseract>=0.3
pypdfium2>=4
markitdown>=0.1
python-docx>=1.1
python-pptx>=1.0
pywin32>=306; platform_system=="Windows"
